import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Use webpack for SVG handling with @svgr
  webpack(config) {
    config.module.rules.push({
      test: /\.svg$/,
      use: ["@svgr/webpack"],
    });
    return config;
  },
  // Turbopack config for SVG handling
  turbopack: {
    rules: {
      '*.svg': {
        loaders: ['@svgr/webpack'],
        as: '*.js',
      },
    },
  },
};

export default nextConfig;
